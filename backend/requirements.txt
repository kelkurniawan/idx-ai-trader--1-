# IDX AI Trader Backend - Dependencies

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0              # Async PostgreSQL driver — used by the running application
alembic>=1.13.0              # Database migration framework
psycopg2-binary>=2.9.9       # Sync PostgreSQL driver — used ONLY by Alembic migration runner
aiosqlite>=0.19.0            # Async SQLite driver — used for local development fallback

# AI Integration (optional, only needed for production)
google-genai>=1.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Environment
python-dotenv>=1.0.0

# HTTP Client (for future IDX API integration)
httpx>=0.24.0

# File upload support
python-multipart>=0.0.6

# ===========================
# Authentication & Security
# ===========================

# JWT token generation and verification
PyJWT>=2.8.0

# Password hashing (bcrypt)
passlib[bcrypt]>=1.7.4

# TOTP (Google Authenticator) support
pyotp>=2.9.0

# QR code generation for TOTP setup
qrcode[pil]>=7.4.0

# Google OAuth token verification
google-auth>=2.20.0

# Redis client (production shared state for OTP and rate limiting)
redis>=5.0.0

# Rate Limiting
slowapi>=0.1.9
