# ── API URL ───────────────────────────────────────────────────
# Option A (Recommended): Connect to Caddy Gateway acting as single entrypoint
VITE_API_URL="http://localhost:8080"

# Option B (Fallback): Leave empty to leverage Vite Dev Proxy directly
# VITE_API_URL=""
